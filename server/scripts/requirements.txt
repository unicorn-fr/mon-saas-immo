# ─────────────────────────────────────────────────────────────────────────────
#  Moteurs OCR — Bailio KYC document scanner
#  Install : pip install -r server/scripts/requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Prétraitement image (OBLIGATOIRE) ──────────────────────────────────────
opencv-python       # correction perspective, bilateral filter, CLAHE, Otsu

# ── Option A : PaddleOCR PP-OCRv5 (RECOMMANDÉ — meilleur 2025/2026) ─────────
# Benchmark : 93% confiance vs EasyOCR 85%, Tesseract 89%
# Source : https://www.codesota.com/ocr/paddleocr-vs-tesseract
#
# paddlepaddle
# paddleocr

# ── Option B : doctr (Mindee open source, MIT License) ──────────────────────
# Même moteur que l'API Mindee payante — gratuit, très précis sur CNI/permis
# python-doctr[torch]      # avec PyTorch
# python-doctr[tf]         # avec TensorFlow
# python-doctr             # avec ONNX Runtime (plus léger)
# onnxruntime              # si option ONNX ci-dessus

# ── Option C : EasyOCR (CRAFT + CRNN) ───────────────────────────────────────
# easyocr

# ── MRZ spécialisé : fastmrz (FORTEMENT RECOMMANDÉ pour CNI) ────────────────
# Bibliothèque dédiée MRZ avec validation ICAO-9303 complète
# pip install fastmrz
# fastmrz

# ── Bonus MRZ : pytesseract (OCR-B natif, améliore encore la MRZ) ───────────
# Requiert aussi : brew install tesseract (macOS) / apt install tesseract-ocr (Linux)
# pytesseract
